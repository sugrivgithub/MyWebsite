// This is an optional script file if you'd like to add interactivity.
document.addEventListener('DOMContentLoaded', () => {
    console.log("Cypress Learning Hub Loaded!");
    
    // Example of dynamically loading content (if needed)
    const learnMoreButton = document.querySelector('.cta-button');
    learnMoreButton.addEventListener('click', () => {
      alert('Let\'s begin learning Cypress!');
    });
  });
  